package content

import java.util.Date;

/** Describes the Articles in 2gocopenhagen. Each Article must have a title,
 * a description, a picture, contact information, comments.For more information refer to the mock-ups.
 * Each article should belong to a category and to a type of a category.
 *
 *@TODO Consider what other fields we need for the article
 *@TODO Think if we need some constrains here
 *
 * @author 2go IT.
 *
 */


class Article extends Content {
	

	/** The constrains of the Event record fields */
	static constraints = {
	}
}
